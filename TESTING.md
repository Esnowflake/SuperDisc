# 编译后的验收步骤（尚未执行）

先在只安装对应 Loader、必要 Fabric API 和本模组的新测试世界中验证，再装入现有整合包。
下表应在 Forge 1.20.1、Forge 1.21.1、NeoForge 1.21.1、Fabric 1.21.1、Fabric 26.2 五个目标上分别用单机和独立服务器的两个客户端执行。
未实测项不能视作已通过。

| 场景 | 操作 | 预期 |
|---|---|---|
| 资源和注册 | 创造栏/命令获取、合成 8 金锭+绿宝石 | 金色绿色唱片可见、产量 1，无缺失资源警告 |
| 原版交互 | 普通唱片右击未绑定唱片机 | 保持原版行为 |
| 界面绑定 | 超级唱片右键空唱片机 | 打开路径/文件按钮、4 个控制按钮、2 个滑块 |
| 解码 | 分别导入 MP3、Ogg Vorbis | 同步后播放，状态有时长，错误文件有提示 |
| 取消选择 | 播放中或同步中再次打开选择器，不选直接关闭 | 路径、音乐、播放位置和同步状态保持不变 |
| 继续/重播 | 暂停后开始，再按重新播放 | 继续原位置；重新播放从零开始 |
| 默认模式 | 播放短音乐至结束 | 停在结尾 |
| 循环模式 | MP3、Ogg 及不足 1 秒的短曲各循环多次 | 原音源连续衔接，无软件插入的等待，GUI 回零；源文件静音原样保留 |
| 音量 | 400%、100%、50%、0%切换，检查游戏音量 | 开启保护时弱信号最多四倍；100% 原音量；半音量；静音，听觉范围不增加 |
| 防失真默认值 | 使用没有该配置项的旧版配置进入游戏 | 防失真默认开启，原音量数值保持原比例 |
| 满幅音频 | 保护开启，使用峰值接近满幅及明显较安静的两首曲目，调到 400% | 高峰值曲目自动限制增益，安静曲目可以放大，不发生放大造成的硬削波 |
| 关闭防失真 | 关闭保护，400% 播放高峰值音乐，再开启保护 | 关闭时实际高增益允许破音，开启后队列更新恢复保护，播放进度不重置 |
| 防失真独立性 | A/B 开关不同，A 远程调整 B 音量 | B 仍按自己的防失真设置输出，A 无法远程关闭 B 的防失真 |
| 保存防失真 | 关闭后退出并重启，随后开启再重启 | 本机偏好保留，源文件及 PCM 缓存不被更改 |
| 尾段计时 | MP3 / Ogg 各选一首末尾有可听声音的音乐，观察最后 5 秒 | 进度与时间一致，末尾声音完整，无提前结束或尾段重复 |
| 极短音频 | 不足 1 秒、3.25 秒及普通长音频分别播完 | 空缓冲不会推动时钟，完成后不会不断重建尾段 |
| 进度焦点 | 拖动进度后保持滑块焦点，再播放至结束 | 滑块继续更新，不因焦点停住，百分比不提前显示 100% |
| 自动同步 | A 选定大文件后不点击播放，B 无缓存 | 自动上传、下载、解码，全部就绪后显示同步完成 |
| 上传中取消 | A 上传期间主动清空路径 | 上传停止，服务器删除对应 `.part`，不会自动播放 |
| 下载/解码中取消 | B 正在下载或解码时，A 清空路径 | B 停止处理并清理对应临时文件；其他唱片机的任务不受影响 |
| 快速取消/换曲 | 刚选曲立即取消，再迅速选另一首 | 旧回包不能恢复被取消任务，新曲正常同步，不误删完整缓存 |
| 删除源文件 | 同步未完成时从资源管理器删除源文件 | 约一秒后取消同步并清理未完成缓存；同步完成后的完整缓存可以继续用 |
| 距离/粒子 | 走近走远、看方块顶部 | 声音衰减，播放时出现音符粒子 |
| 联机首次同步 | A 导入，B 无缓存，A 开始播放 | 所有人 ready 后开始；每秒有同步进度和完成通知 |
| 缓存命中 | 重连后用同一文件名和内容 | 直接使用缓存，无重复文件传输 |
| 同名不同内容 | 两首不同音频改成同名分别导入 | 内容哈希不同，不误播放缓存 |
| 进度权限 | B 放置、A 导入、B 拖进度/A 拖进度 | B 禁止，A 可用；服务器也验证 |
| 个人音量 | A 播放，B 调主面板音量 | 只有 B 的音量变化，A 不变 |
| 管理权限 | A 播放，A/B 分别看管理入口 | A 可进入独立管理界面；B 禁用，不能绕过网络权限修改 A |
| 远程音量 | A 在管理页拖动 B 的滑块 | B 的音量及主界面数值更新，A 音量不变 |
| 音量保护 | B 开启保护，A 尝试调整，随后 B 自行调整 | A 不能调整 B，滑块灰色；B 自己可调，A 面板看到更新值 |
| 管理交接 | A 播放时 B 点击开始；再让 B 暂停后继续/重播 | 第一步 A 保留管理权，后续 B 获得管理权；进度仍归导入者 |
| 独立界面 | A 管理页返回主面板，重复多次 | 操作权限保留，播放不停止；退出后停止列表订阅 |
| 多人列表 | 多人进出、滚动、B 在 A 拖滑块期间开启保护 | 在线名单更新；受保护滑块立即禁用，服务器拒绝迟到修改 |
| 路径引号 | 粘贴 Windows 带双引号的含空格路径，再开始 | 过滤两端引号，成功导入 |
| 保存与重连 | 调整个人音量/保护，退出重进及重启服务器 | 个人音量按唱片机保存，保护偏好恢复 |
| 性能 | 多台同时播同一文件，连续循环，开关管理页 | 缓存复用；循环计时内存不随时长无限增长；无列表持续广播 |
| 中途加入 | A 播放两分钟后 B 进服/靠近 | B 同步完加入当前播放位置 |
| 多播放器 | 两台唱片机放两首不同音乐 | 独立音源、位置、进度和音量 |
| 断线 | 上传者断网、听众离线、主人离线 | 清理/超时提示；离线听众不永久阻塞；已播曲用服务器时钟续播 |
| 世界退出 | 断线重连或切世界 | 旧音源停止，不串到新世界 |
| 音频重载 | 切设备或 F3+T | 资源恢复后按当前进度重新建立音源 |
| 清除设备 | 破坏、解除绑定、区块卸载 | 停止相关音乐，普通唱片机可重新使用 |
| 独立服务器 | headless Linux/Windows 对应 Loader 服务端 | 不加载客户端界面、LWJGL/解码类，不崩溃 |

服务端与客户端需安装相同目标的相同版本安装 JAR。若同步失败，保留两端 `logs/latest.log`，记录文件格式、时长、参与人数及操作时间。

## 多平台验收状态

`2.1.0-alpha.2`：五个目标均有完整构建、核心回归及静态安装包校验。
1.21.1 和 Fabric 26.2 另有存档和声音接口静态契约检查。
上述游戏内操作仍需逐目标验收；不要把自动测试结果当作游戏实测。
Fabric 的实际 Mixin 注入、各目标音频设备播放、专用服务端和双客户端同步尚未验收。
使用测试世界，先备份旧存档；不同 Loader/游戏版本之间不承诺存档互转。
